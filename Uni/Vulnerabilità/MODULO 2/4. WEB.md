## HTTP REMINDER
Nella parte di http c'è un terzo layer detto di "header" che nelle slide non si vede
- http è stateless -> ogni richiesta è indipendente dall'altra (anche dello stesso client)
- Utilizzando dei cookie possiamo identificare l'utente che fa una richiesta
	- non è stateless quindi

PHP VIENE ESEGUITO SOLO SUL SERVER
JAVASCRIPT VIENE ESEGUITO SOLO SUL CLIENT


## Regole da rispettare
- NON FIDARTI MAI DEL CLIENT
	- anche se si tratta di una mega gotica culona (anzi stai molto attento proprio a quella)


## Enumeration
Trovare le pagine più interessanti di un'applicazione web

#### Metodi
1. vado sul sito e esploro le pagine (semplice ma comunque efficace)
	- posso vedere anche se le singole pagine hanno già internamente delle referenze ad altre pagine
2. uso dei tools
	- codici di errore HTTP
	
| **Codice**              | **Descrizione**                                                                  |
| ----------------------- | -------------------------------------------------------------------------------- |
| **1xx** (Informational) | Messaggi informativi, raramente usati.                                           |
| **2xx** (Success)       | La richiesta è andata a buon fine.                                               |
| **3xx** (Redirect)      | Il client deve fare un’altra richiesta perché la risorsa si è spostata.          |
| **4xx** (Client Error)  | La richiesta contiene un errore (es. risorsa non trovata, richiesta non valida). |
| **5xx** (Server Error)  | Errore lato server, il server non ha potuto soddisfare la richiesta.             |
- Faccio una richiesta con `GET` e in base al codice che ricevo mi regolo.

Negli applicativi web esiste un file che si chiama `robots.txt` 
- è un file accessibile da tutti che contiene informazioni interessanti
	- VEDI DALLA SLIDE

- PARAMETRO HOST -> se cambiato mi permette di cambiare pagina WEB anche se sono sulla stessa porta


## Burpsuit
Funziona come meccanismo di proxi



## CVE
Server per trovare vulnerabilità in un server e ricavarne possibili exploit
- posso cercare `server cve` su google e cercare a cosa è vulnerabile un determinato server

Gli exploit si trovano su www.exploit-db.com

