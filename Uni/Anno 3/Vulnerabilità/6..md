# Linux Offensive Security – Exploitation
## 1. Finding Attack Vector
### Cos’è
È la fase in cui cerchi **come ottenere privilegi più alti** dopo l’accesso iniziale.
👉 Non è più solo raccogliere info (enumeration), ma:
- trovare **punti deboli sfruttabili**
- scegliere **come attaccare il sistema**

### Differenza con Enumeration
- **Enumeration** → raccolgo info (utenti, servizi, permessi)
- **Attack vector** → uso quelle info per **eseguire un attacco reale**


## 2. Kernel Exploit
### Definizione
Programmi che sfruttano vulnerabilità del kernel per ottenere privilegi root.
### Quando usarli
- se il sistema **non è aggiornato**
- versione kernel vulnerabile
### Tool
- `searchsploit` → cerca exploit già pronti

### Nota importante
⚠️ Non usarli come prima scelta
- possono rompere il sistema
- sono rumorosi
- poco “stealth”


## 3. Dirty COW (esempio)
### Idea
Permette di **modificare file protetti** → escalation a root
### Cosa può fare
- sostituire root
- modificare `/etc/passwd`

### Perché non usarlo subito
- instabile
- facilmente rilevabile


## 4. Privilege Escalation – Tecniche principali
### 4.1 Modifica `/etc/passwd`
Aggiungi un utente con privilegi root:
- crei hash password
- inserisci riga in `/etc/passwd`

![[Pasted image 20260325192220.png]]
👉 risultato: nuovo utente root

### 4.2 Modifica sudoers
![[Pasted image 20260325192247.png]]
👉 può eseguire tutto con `sudo` senza password

#### Sintassi
![[Pasted image 20260325192311.png]]

Esempio:
```
sudo cat /etc/passwd
```

### 4.3 SUID (Set User ID)
### Idea
Eseguire un file come se fossi root

Esempio:
- copi bash
- dai permessi `4777`

![[Pasted image 20260325192505.png]]
👉 il **4 = SUID**
> scrivere `4` o scrivere `s` è la stessa cosa

Risultato:
- shell con privilegi elevati

## 5. Credential Dumping
### File importanti
- `/etc/passwd`
- `/etc/shadow`

### Procedura
1. leggi i file
2. unisci con `unshadow`
3. crack offline (es. john)

![[Pasted image 20260325192735.png]]
👉 attacco offline → più sicuro per l’attaccante


## 6. Path Hijacking
### Idea
Sfrutti come il sistema cerca i comandi

### Come funziona
1. c’è uno script (es. usa `curl`)
2. non hai permessi per eseguirlo direttamente
3. scopri il PATH
4. crei un comando con lo stesso nome
5. lo fai eseguire al posto di quello originale

![[Pasted image 20260325192900.png]]
👉 esegui codice malevolo


## 7. Wildcards Exploit
Uso di `*` negli script.

### Problema
- espande file in modo automatico
- può includere file malevoli

👉 puoi manipolare il comportamento dello script


## 8. Sudoers Path Traversal
### Scenario
![[Pasted image 20260325193433.png]]
Puoi usare solo:
```
cat /opt/scripts/*
```

### Problema
Vuoi lefggere leggere `/etc/shadow` ma non puoi.

### Bypass
Usi:
```
../
```
👉 vuol dire TORNA ALLA DIRECTORY GENITORE DI QUELLA ATTUALE.

Esempio:
```
/opt/scripts/../../../../etc/shadow
```
👉 da `/opt/scripts` torna indietro di 5 cartelle (che è la `root`) e poi entra in `/etc/shadow`


## 9. Argument Injection (7zip)
![[Pasted image 20260325194309.png]]
### Idea base
Alcuni programmi (tipo 7zip) interpretano certi simboli in modo speciale.

👉 `@file` **NON è un file normale**  
vuol dire:  
“leggi da questo file la lista dei file da usare”

### Cosa succede davvero
### 1. Tu crei un file
```bash
touch @tosteal
```

### 2. Dentro ci metti:
```bash
/etc/passwd
```

👉 quindi `@tosteal` contiene una lista di file (in questo caso 1 solo)

### 3. Qualcuno esegue:
```bash
7za a backup.7z *
```

👉 qui c’è il punto chiave:
- `*` prende **tutti i file nella cartella**
- tra questi c’è anche `@tosteal`

### Il trick
Quando 7zip vede:
```
@tosteal
```

NON lo tratta come file normale
👉 lo interpreta come:
> “usa il contenuto di questo file come input”

### Risultato
7zip legge dentro `@tosteal`:
```
/etc/passwd
```

👉 quindi aggiunge **/etc/passwd** al backup
⚠️ anche se:
- non era nella cartella
- non doveva essere incluso


## 10. Python exploit
Ci sono diverse tecniche di exploitation legate a python
### Python2 – input vulnerability**
   - in python2 la funzione `input()` è pericolosa
   - interpreta direttamente l’input come codice
   - esempio:
	- __import__('os').system('/bin/bash')
	       👉 permette di eseguire comandi arbitrari (anche come root se lo script è privilegiato)
    - **module override (import hijacking)**
        - quando python fa:
            `import modulo`
        - cerca il modulo in questo ordine:
            1. directory corrente
            2. moduli built-in
            3. PYTHONPATH
            4. librerie standard
            5. site-packages
        👉 se riesco a scrivere in una directory “prima”:
        - posso creare un modulo malevolo con lo stesso nome
        - python importerà il mio codice
        👉 risultato:
        - esecuzione di codice con privilegi elevati

### Python3 – script replacement
- se uno script python viene eseguito con sudo:
    `sudo python3 script.py`
- e io posso modificarlo o sostituirlo:
    ```
    import os  
    os.system('/bin/bash')
    ```
 👉 ottengo una shell privilegiata


## 10. Cronjobs
### Definizione
Task schedulati che vengono eseguiti automaticamente (spesso come root)

### script cronjob scrivibile
Se trovo uno script eseguito da `cron` con più privilegi:
   - e ho permessi di scrittura
        👉 posso modificarlo e far eseguire:  
        - comandi miei  
        - reverse shell  
        - aggiunta utente root  
        - SUID backdoor
    - idea chiave:
        - non devo “bucare” nulla
        - faccio eseguire al sistema il mio codice automaticamente
        - sfrutto i privilegi del proprietario del cronjob

### Insecure binary permissions
Alcuni binari di sistema hanno **permessi elevati** (SUID, SGID, sudo, capabilities)
   - servono per operazioni legittime (es. cambiare password)
    👉 problema:
   - se il binario permette:
        - shell escape
        - lettura/scrittura file arbitrari
    - può essere abusato per ottenere root
    - cose da controllare:
        - `sudo -l`
            - binari eseguibili come root
        - SUID binaries:
            ```bash
            find / -type f \( -perm -u+s -o -perm -g+s \) -exec 
            ls -l {} \; 2>/dev/null
            ```
        - capabilities:
            `getcap -r / 2>/dev/null`
    👉 idea:
	    - trovare binari “potenti” e capire **come abusarli**

### Exploiting Capabilities
   - le capabilities sono tipo:  
        👉 “root ma solo per una cosa specifica”
    - invece di dare root completo:
        - danno singoli privilegi (es. leggere file, cambiare uid)
    ⚠️ problema:
    - alcune capabilities sono **equivalenti a root**
- esempi importanti:  
- **cap_setuid**  
	- permette di cambiare UID (diventare root)  
	```bash  
	python3 -c 'import os; os.setuid(0); os.system("/bin/bash")'  
	```  
	👉 shell root   
- **cap_dac_read_search**  
	- permette di leggere file ignorando permessi  
	```bash  
	tar -czf /tmp/shadow.tar.gz /etc/shadow  
	```  
	👉 leggo `/etc/shadow`     
- **cap_dac_override**  
	- permette di bypassare controlli di accesso (scrittura inclusa)  
	```bash  
	vim /etc/passwd  
	```  
	👉 posso modificare file di sistema   
👉 idea chiave:  
- capabilities = “root-lite”  
- ma alcune → escalation completa
- **GTFOBins**
    - sito: [https://gtfobins.org/](https://gtfobins.org/)
    - contiene:
        - tecniche per sfruttare binari già presenti nel sistema
    - permette:
        - privilege escalation
        - file read/write
        - shell escape







