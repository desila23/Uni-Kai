In questa lezione scopriamo cosa c'è oltre pane e pizza.


### FINDING ATTACK VECTOR
- in cosa consiste
- e come si differenzia da ENUMERATION

### Kernel Exploit
Sono programmi che attaccano alcune versioni non aggiornate del kernell
Ci sono tool come `searchploit`
- per cercare exploit già presenti in un database


### Dirty cow
- permette di sostituire l'utente root come un'altro 
- NON USARLO COME PRIMA SCELTA
	- vedi perché


## Categorie - PRIVILEDGE COMMAND EXECUTION
### adding a fake root account to `/etc/passwd`![[Pasted image 20260325141910.png]]

### adding current user to sudoers![[Pasted image 20260325142051.png]]
- l'utente eve può usare ogni singolo comando presente nel sistema senza alcun limitazione
	- DEVO METTERE PRIMA DI OGNI COMANDA `sudo`
```
sudo cat /etc/passwd
```


### creare una finta bash con SUID
![[Pasted image 20260325142456.png]]
- il `4` in `4777` indica la `s` in `suid`


### esfiltrare il file `shadow` e iniziare a crakkare offline
![[Pasted image 20260325143022.png]]


### Path Hijacking
Qui credo l'idea sia
- c'è uno script in cui è presente curl MA non posso eseguirlo perché ho i privilegi troppo bassi
- Scopro da dove va a prendere le informazioni
- scrivo un comando con lo stesso nome ma che fa ciò che voglio io
- aspetto che qualcuno esegua lo script modificato


### Trick con le wildcards
#### Sudoers path traversal
![[Pasted image 20260325144322.png]]
Qui mi dice che posso eseguire solo cat MA nella cartella `/opt/scripts/*`.
Se voglio fare cat per vedere il contenuto di `/etc/shadow` NON POSSO FARLO.

Però posso tornare forzatamente alla root e poi entrare dentro `/etc/shadow`.
Uso `../` ripetuto -> vuol dire TORNA ALLA DIRECTORY GENITORE DI QUELLA ATTUALE.
![[Pasted image 20260325144526.png]]
Qui dico
- da `/opt/scripts` torna indietro di 5 cartelle e poi entra in `/etc/shadow`

#### 7z argument injection
- ingannare 7zip per fargli interpretare male i nomi dei file
	- slide 21
		- creo un file `@tosteal` con touch, dentro ci scrivo una stringa con `echo "/etc/passwd" > tosteal`
		- `7za a backup.7z -t7z -snl *`
			- creando il backup interpreta il file `@tosteal` come una riga da eseguire, quindi fa il backup di `shadow`
			- la @ viene usata per definire una lista di file da eseguire

#### Bash script wildcard and input vulnerabilities
- posso usare negli script ad esempio `*`
- per far dare corrispondenza di password, in questo caso `db pass` è uguale a `user pass`


### Python2
- VEDI


### Cronjobs
- Task ripetitivi
Se ho i permessi di scrivere su un cronjobs posso modificare il suo script
- alcuni cronjob possono essere triggerati al login dell'utente, e anche in altri casi


