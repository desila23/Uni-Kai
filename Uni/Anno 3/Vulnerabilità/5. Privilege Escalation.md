- `echo $SHELL` -> ?
- `echo $0` -> ?

Alcune bash sono restricted -> è una buona norma averla
- `bash` / `/bin/bash` -> ?

- `vi` 
- `vim`

- `/bin/sh`

- `vim -c '!/bin/bash'`

- Posso usare nano/vim/vi per usare gli editor di testo e poi usare i comandi (se la bash è ristretta e non posso accedere).
	- Posso farlo scrivendo
		- `:! <command>`
		- CTRL + T and write command

- `script` -> 
	- `script -qc /bin/bash /dev/null` -> altro modo per accedere a una shell
		- al posto di `/bash` posso mettere anche `/zsh` e altre mille bash A SECONDA DELLA BASH CHE TROVO DOPO AVERE FATTO `echo $SHELL` / `echo $0

Alcune volte ci sono dei controlli che guardano anche proprio i comandi che usi ma posso anche "scrivere male" e il comando funziona lo stesso
- ESEMPI
	- `/bin/b?sh`
	- `/bin/b[a]sh`
	- `/bin/b*sh`
	- `p${u}i${u}n${u}g` -> la bash sostituisce alla variabile d'ambiente `u` un valore nullo (la cancella), quindi poi rimane solo `ping` e lo esegue

Posso anche prendere un comando (es. `whoami`), dividendolo in 
`wha` e `mi`, scrivendoli sulla shell e "fondendoli"
![[Pasted image 20260319102458.png]]
- `!-1` -> esegui l'ultimo comando che ho scritto
- `!-2` -> esegui il penultimo comando che h

Anche qui posso usare le variabili d'ambiente `u`
```
w'u'h'u'o'u'a'u'm'u'i
```


- `IFS` -> variabile d'ambiente che definisce "spazio"
	- posso usarlo per fare tipo `cat /etc/passwd` quando non posso LETTERALMENTE usare lo spazio
	- diventerebbe `cat${IFS}/etc/passwd`
	- FUNZIONA SOLO SU BASH

- `IFS=] b=/bin/bash];$b` -> esegue `b` e poi sostituisce `]` con IFS e quindi "spazio"

- `{COMANDI}` -> ?

- se non posso mettere gli spazi posso usare la redirezione `<`

- posso anche scrivere i comandi in esadecimale e lanciarli
	- se terminale posso usare xxd per trasformarlo
	- oppure cerchi su """"""""""internet""""""""""
	- `/x` indica lo spazio



## PRIVILEGE ESCALATION
È sistematico 
1. enumerazione -> devo capire cosa posso fare con la macchina
2. Pisello
3. Sborra


#### MOTD
Tipo quando accedo con SSH ho il messaggio che viene stampato (tipo quello di bandit

- `uname -a` -> all
	- molti altri `-...`

