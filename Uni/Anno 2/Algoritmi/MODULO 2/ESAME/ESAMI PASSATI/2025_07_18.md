![[Pasted image 20250905162043.png]]
Dato che `e` $\in$ `T` vuol dire che è l'arco più leggero che attraversa una cutset
	Sia Kruskal che Prim, aggiungono un arco a `T` se e solo se rispetta questa proprietà.
Se viene costruito G' abbassando il peso di `e`, la proprietà viene ancora rispettata (e anzi migliorata) e quindi l'arco continua a far parte di `T`.
Quindi `T` è un MST anche di G'.


![[Pasted image 20250905162540.png]]
1. Dato una flow network $G = (V, E, s, t, c)$, si definisce `cut` una partizione di nodi `(A, B)` con $s \in A$ e $t \in B$ e la sua capacità è la somma totale della capacità massima dei nodi uscenti da A. 
	In questo contesto si vuole trovare un taglio con capacità minima in modo tale da selezionare quegli archi "collo di bottiglia" e trovare un conseguente valore massimo per il flusso uscente da `s`.
	- Ricordiamo che per ogni arco `e` -> $0 \le f(e) \le c(e)$ -> quindi se trovo un taglio minimo ho anche un upper bound per il valore da assegnare al flusso.

2. BFS da `s` a `t`
3. Toerema cammini aumentanti e MAx-Flow min-cut


![[Pasted image 20250905163502.png]]
![[Pasted image 20250905165838.png]]![[Pasted image 20250905165856.png]]














