# ESERCIZIO 1
![[Pasted image 20250905170023.png]]
1. **FALSA** -> è AL MASSIMO UGUALE
2. **FALSA** -> l'algoritmo è PSEUDO POLINOMIALE nella dimensione dell'input, anche con valori interi
3. VERA
4. FALSO -> posso aumentarla di $c(e)$ solo se `e` è il collo di bottiglia

![[Pasted image 20250905175235.png]]
1. **FALSO** -> l'euristica union by size per la QUICKUNION garantisce che l'altezza dell'albero non cresca troppo ma può essere ANCHE maggiore di 1
2. **FALSO** -> nel caso peggiore ho $(O(n))$ perché eseguo `n-1` union
3. **FALSO** -> CASO PEGGIORE = $O(m + n^{2})$
4. VERO
5. VERO


![[Pasted image 20250906144955.png]]
1. **FALSA** -> se i pesi sono uguali esistono `n-1` (credo) MST, quindi non è sempre vero
2. VERO
3. VERO
4. **FALSO** -> questa proprietà vale per `M`, per `S` non esiste la cycle property
5. VERO -> se è aciclico già è un albero 


![[Pasted image 20250906164208.png]]
1. FALSO -> belman-ford trova i cammini minimi, non è detto che mi restituisca un MST
2. FALSO -> se quell'arco è l'unico con cui raggiungo un dato nodo lo prendo
	- GENERALMENTE non lo contiene
3. FALSO -> se tutti gli archi pesano 1 non vale
4. FALSO -> pensa all'albero pokeball con `1` e `0,1`
5. VERO -> l'MST è unico se tutti i pesi sono uguali (unico caso in cui i due alberi sono identici)


![[Pasted image 20250907104601.png]]
1. FALSO -> sempre MINORE O UGUALE
2. VERO
3. FALSO -> stesso ragionamento della 1
4. FALSO -> è sempre pseudo-polinomiale
5. VERO -> conservazione del flusso


![[Pasted image 20250907112611.png]]
1. FALSO -> nella quick find ogni albero ha altezza al massimo 1, l'euristica serve per non far cambiare troppe volte padre a un nodo
2. FALSO -> nel caso peggiore le union costano $O(n)$, ripetuto per `n-1` volte diventa $O(n^{2})$
3. VERO
4. VERO
5. VERO


![[Pasted image 20250907115633.png]]
1. VERO -> MST unico se ha pesi distinti, nel caso in cui non lo siano ci sono diversi MST ma il peso è identico con entrambi gli algo
2. FALSO
3. VERO
4. VERO (cut e cycle property)
5. FALSO -> posso dire che è l'arco più leggero del CUTSET, non del ciclo.



![[Pasted image 20250905170543.png]]
![[Pasted image 20250905180433.png]]


![[Pasted image 20250905162043.png]]
Dato che `e` $\in$ `T` vuol dire che è l'arco più leggero che attraversa una cutset
	Sia Kruskal che Prim, aggiungono un arco a `T` se e solo se rispetta questa proprietà.
Se viene costruito G' abbassando il peso di `e`, la proprietà viene ancora rispettata (e anzi migliorata) e quindi l'arco continua a far parte di `T`.
Quindi `T` è un MST anche di G'.


![[Pasted image 20250906145403.png]]
L'affermazione è FALSA.
Si consideri un cammino `s` -> `t` diretto di peso 20 e si consideri un altro cammino `s` -> `u` -> `t` di peso 21 (con `s` -> `u` = `19` e `u` -> `t` = 2.
- L'MST prende il cammino `s` -> `u` -> `t` = `21`
- L'SPT prende `s` -> `u` = `19` + `s` -> `t` = `20` -> `39` (perché vuole minimizzare i singoli cammini e non il costo totale)

Quindi il peso massimo raggiungibile da SPT è MENO del doppio del peso dell'MST.


![[Pasted image 20250906164729.png]]
L'affermazione è VERA.
Un MST ha al più `n-1` archi (perché altrimenti creerei un ciclo).
Se, nel caso peggiore, ho esattamente 3 archi con costo 1, vuol dire che il costo totale sarà dato da $$2n-1 - 3(1) = 2n - 4$$


![[Pasted image 20250907104904.png]]
L'affermazione risulta vera.
In ford-fulkerson, data una capacità massima `C`, verranno eseguire ALMENO `C` iterazioni.
Però se tutte le capacità degli archi sono uguali a `1`, questo vuol dire che ogni aumento su un cammino aumentante renderà saturo il cammino forward, il che vuol dire che avremo un numero di iterazioni AL MASSIMO pari al numero di archi, quindi faremo al massimo `2m` aumenti.
Dato che possiamo trovare un cammino aumentante tramite una visita BFS (con complessità $O(m$)
La complessità temporale totale sarà $$T(n) = O(mn)$$


![[Pasted image 20250907113543.png]]
Con la union by size, l'albero con cardinalità più piccola diventerà il figlio dell'altro insieme.
Per avere un altezza di $\theta(log(n))$ possiamo eseguire
- `makeset(1), ..., makeset(n)`
- uniamo a coppie gli insiemi ottenuti -> `union(1,2), union(3,4), ...` così da ottenere alberi di altezza `1` e cardinalità `2`
- uniamo ora a coppie gli alberi ottenuti, così da avere alberi di altezza `2` e cardinalità `2`
- ripetiamo, così unendo alberi di cardinalità $2^{k}$ otteniamo alberi di cardinalità $2^{k+1}$
- dopo $h = log(n)$ livelli, abbiamo un unico albero di cardinalità `n` e altezza esattamente $\theta(log(n))$



![[Pasted image 20250907120559.png]]
L'affermazione risulta falsa.
Data la cut property, se `e` appartiene all'MST allora è il minimo di ALMENO un taglio che attraversa, ma non abbiamo garanzie che sia il minimo di tutti i tagli.
Ad esempio, se `e` è l'arco di peso massimo del grafo ma è anche l’unico che collega un nodo isolato, entra per forza nell’MST MA in altri tagli che attraversa non sarà l’arco minimo.


---
# ESERCIZIO 2

![[Pasted image 20250905170813.png]]
1. Dato un grafo $G = (V, E, w)$, non orientato e pesato, si vuole trovare un sottografo contenente tutti i nodi il cui peso totale (quindi somma di tutti gli archi presi) sia MINIMO.
	- il sottografo deve essere aciclico e connesso
2. Sia `S` un taglio di `G` e sia `e` un arco di costo minimo facente parte del cutset (archi con esattamente un estremo in `S`). Allora `e` far parte di un qualche MST di `G`
	- si dimostra per exchange argument ipotizzando che `e` non faccia parte del cutset
3. L'algoritmo di prim basa la sua correttezza sul fatto che viene esiguita per `n-1` volte la cut property
	- l'idea di prim è partendo da un nodo, vengono osservati tutti gli archi uscenti e viene selezionato poi come nodo successivo quello che ha l'arco entrante di peso minimo.
	- Quindi viene scelto ogni volta un arco che ha esattamente un estremo nell'insieme di nodi già presi, e questo insieme non è altro che un cut (e l'arco fa parte quindi del cutset)
	- Il tutto, come detto in precedenza, è ripetuto `n-1` volte


![[Pasted image 20250905162540.png]]
1. Dato una flow network $G = (V, E, s, t, c)$, si definisce `cut` una partizione di nodi `(A, B)` con $s \in A$ e $t \in B$ e la sua capacità è la somma totale della capacità massima dei nodi uscenti da A. 
	In questo contesto si vuole trovare un taglio con capacità minima in modo tale da selezionare quegli archi "collo di bottiglia" e trovare un conseguente valore massimo per il flusso uscente da `s`.
	- Ricordiamo che per ogni arco `e` -> $0 \le f(e) \le c(e)$ -> quindi se trovo un taglio minimo ho anche un upper bound per il valore da assegnare al flusso.

2. BFS da `s` a `t`
3. Toerema cammini aumentanti e MAx-Flow min-cut


![[Pasted image 20250905175838.png]]
1. Dati in input `n` intervalli (lezioni), ognuno con un tempo di inizio $s_i$ e un tempo di fine $f_{i}$, si vuole trovare un numero di classi minimo per schedulare ogni lezione, sapendo che in ogni classe le lezioni devono essere MUTUALMENTE COMPATIBILI
2. La *depth* si riferisce al numero di intervalli che si sovrappongono in un determinato istante di tempo `i`
	- Osservazione chiave: Numero di classi $\ge$ Depth
	- L'algoritmo greedy (*earliest START time*) riesce a utilizzare un numero di classi esattamente pari alla depth, e quindi è ottimo.
		- Questo perché se apre una classe `d`, vuol dire che quella lezione era incompatibile con TUTTE LE ALTRE LEZIONI PRESENTI NELLE `d-1` classi 
		- Il che vuol dire che anche le lezioni, tra di loro, erano in qualche modo incompatibili in quel dato istante di



![[Pasted image 20250906145834.png]]
1. Dati in input un insieme `n` intervalli, ognuno con un tempo di inizio e fine, si vuole trovare un sottoinsieme di cardinalità massima di intervalli MUTUALMENTE COMPATIBILI, ossia che per ogni coppia di intervalli $l_{i}, \ l_{j}$, con `i` < `j`, $f_{i} \le s_{j}$
2. I job vengono ordinali per TEMPO DI FINE, e un job viene aggiunto al sottoinsieme `S` solo se è compatibile con l'ultimo job selezionato.
	- OSS: ordinare per tempo di fine fa sì che il primo job post ordinamento possa essere preso in modo sicuro, in quanto non ha job che lo precedono.
3. Siano 
	- $i_{1}, ..., i_{k}$ i job selezionati dall'greedy
	- $j_{1} , ..., j_{m}$ i job selezionati dall'ottimo
	Definiamo come $f(i_{r})$ il tempo di fine dell'`i`-esimo job 
	Allora $f(i_{r}) \le f(j_{r})$ e quindi il numero di job presi dal greedy è lo stesso dell'ottimo.
	Da questo lemma deriva il fatto che il greedy sia un algoritmo ottimo.
	
	Per dimostrarlo, supponiamo per assurdo che non lo sia.
	Allora 
	- `m>k` e quindi questo vuol dire che il numero di job presi dall'ottimo è maggiore di quello del greedy (cosa non possibile dato il lemma).



![[Pasted image 20250906165403.png]]
1. Date in input
	- `m` macchine
	- `n` $\ge$ `m` job ognuno con processing time
	Trovare una distrubizione di job affinché il makespan sia minimo.
	Dove per makespan si intende il carico (LOAD) massimo tra tutte le macchina
2. ECCOLI
	LEMMA 1: per ogni `k`, il makespan ottimo è $$L^{*} \ge t_{k}$$questo perché se `k` è il job più pesante, una macchina dovrà processarlo e quindi ALMENO sarà carica quando $t_{k}$
	
	LEMMA 2: il makespan ottimo è $$L^{*} \ge \frac 1 m \sum\limits_{k} t_{k}$$dove la sommatoria indica il processing time totale. 
	Una volta preso questo valore, se lo dividessimo equamente su tutte le macchine, vuol dire che una macchina non può fare MENO di questo valore.
	
	Questi lemmi sono molto importanti perché permettono di dimostrare che l'algoritmo proposto sono un $2$-approssimazione.



![[Pasted image 20250907110845.png]]
1. Formalmente
	3-SAT: data una formula in forma congiuntiva normale (quindi una congiunzione di clausole, ogni clausola con esattamente 3 letterali), trovare, se esiste, un assegnazione che renda la formula `vera`.
	INDIPENDET SET: dato un grafo $G = (V,E)$ non orientato e un intero `k`, trovare, se esiste un sottoinsieme $S \subseteq V$ con $|S| \ge k$ tale che nessuna coppia in `S` è collegata da un arco.

2. Sapendo che `3-SAT` $\le_{P}$ `INDIPENDET-SET`, potremmo costruire un grafo, in cui
	- ogni letterale di una clausola è collegato agli altri tramite un arco (andando a formare un triangolo per ogni clausola)
	- colleghiamo ogni letterale con la sua negazione (presente in un'altra clausola)
	In questo modo, cercare un indipendent set con il grafo trovato equivale a scegliere esattamente un letterale come `vero` per ogni clausola, ottenendo una soddisfazione della formula.



![[Pasted image 20250907114037.png]]
1. Dato un problema polinomiale `Y`, un problema `X` di "riduce polinomialmente a `Y`" se
	- riesco a trasformare ogni istanza di `X` in un'istanza di `Y` in tempo polinomiale
	- riesco a ottenere la soluzione di `X` combinando le soluzioni delle istante in tempo polinomiale.
	Sintatticamente scriverò $X \le_{P} Y$

2. Posso sfruttare questo concetto perché se $X \le_{P} Y$ allora
	- se `Y` so risolverlo in tempo polinomiale, allora saprò risolvere anche `X`
	- se `X` NON so risolverlo in tempo polinomiale, allora nemmeno `Y`
	Molto importante in questo contesto è la proprietà transitiva, ossia data l'implicazione iniziale, se so che $Y \le_{P} W$, i due punti di prima si estendono anche a `W`.



![[Pasted image 20250907120559.png]]
L'affermazione risulta falsa.
Data la cut property, se `e` appartiene all'MST allora è il minimo di ALMENO un taglio che attraversa, ma non abbiamo garanzie che sia il minimo di tutti i tagli.
Ad esempio, se `e` è l'arco di peso massimo del grafo ma è anche l’unico che collega un nodo isolato, entra per forza nell’MST MA in altri tagli che attraversa non sarà l’arco minimo.


---

# ESERCIZIO 3
![[Pasted image 20250905171446.png]]
![[Pasted image 20250905175108.png]]


![[Pasted image 20250905163502.png]]
![[Pasted image 20250905165838.png]]![[Pasted image 20250905165856.png]]


![[Pasted image 20250905180602.png]]
L'idea è molto simile all'indipendent set.
- Uso un array e inserisco tutte le vincite possibili
- E chiaramente posso prendere una cella ogni 4

- `OPT[1] = w_1`
- `OPT[2] = max{w_1, w_2}`
- `OPT[3] = max{w_1, w_2, w_3}`
- DA `i` = 4 in poi
	- `OPT[i] = max{OPT[i-1], w_i + OPT[i-4]}`

Poi per il find solution uso OPT e controllo quali ho preso.


![[Pasted image 20250906150706.png]]



![[Pasted image 20250906170139.png]]
- Per ogni esame vedo quanti giorni impiego a prepararlo
	- CALCOLO `p(j)`
- SOTTOPROBLEMI
	- `OPT(i) = max{OPT[i-1], c_i + OPT[p(j) - 1]`



![[Pasted image 20250908154749.png]]
SOTTOPROBLEMA: `OPT[i, j]` corrisponde al valore guadagnato dopo essere arrivata alla `i`-esima posizione di `A` e alla `j`-esima posizione di `B`.

Ho tre casi principali
- seleziono `i` -> guadagno $a_{i}$ e calcolo la soluzione `OPT[i+1, j+2]`
- seleziono `j` -> guadagno $b_{j}$ e calcolo la soluzione `OPT[i+2, j+1]`

Quindi `OPT[i,j]` = $max\{a_{i} + OPT[i+1, j+2], b_{j} + OPT[i+2, j+1]\}$
Con casi base
- `OPT[0,0]` = $max\{a_{0}, b_{0}\}$ 



![[Pasted image 20250908151113.png]]
Dato che posso prendere ogni volta o il primo pezzo o l'ultimo (i due esterni), la soluzione prevedere che io scelga con cura quale tra i due selezionare.

Se ho un intervallo `(i, j)` ho due casi
- se prendo `i` -> ottengo il grado di godimento $g_{i}$ e alice e bob toglieranno i pezzi `i+1` e `j`
- se prendo `j` -> ottengo il grado di godimento $g_{j}$ e alice e bob toglieranno i pezzi `i` e `j-1`

QUINDI
- `OPT[i, j]` = $max\{g_{i} + OPT[i+2, j+1], g_{j} + OPT[i+1, j-2]\}$