![[Pasted image 20250625115905.png]]
```c
Speciale(v, antenato_speciale) -> discendente_speciale
	if(v == NULL) then return 0
	
	if(v.speciale == True) then
		return Speciale(sx(v), v.val)
		return Speciale(dx(v), v.val)
	
	
```