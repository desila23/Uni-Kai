![[Pasted image 20250624190141.png]]
```c
Sbargis(v, b, g, b_count, g_count)
	if(u == NULL) then return 0
	
	if(b_count >= b OR g_count >= g)
		if(v.col == B) then
			return 1 + Sbargis(sx(v), b, g, b_count+1, g_count) + Sbargis(dx(v), b, g, b_count+1, g_count)
		else 
			return 1 + Sbargis(sx(v), b, g, b_count, g_count+1) + Sbargis(dx(v), b, g, b_count, g_count+1)
	
	else
		if(v.col == B) then
			return Sbargis(sx(v), b, g, b_count+1, g_count) + Sbargis(dx(v), b, g, b_count+1, g_count)
		else 
			return Sbargis(sx(v), b, g, b_count, g_count+1) + Sbargis(dx(v), b, g, b_count, g_count+1)


Chiamata_Sbargis(r, b, g)
	if(r == NULL) then return 0
	
	return Sbargis(r, b, g, 0, 0)
```


# ESERCIZIO 3
![[Pasted image 20250624194129.png]]
```
Girgos(a_1,...,a_n)
	AVL <- INSIEME VUOTO
	k = rad(n)
	
	for i = 1 to k do
		AVL.insert(a_i)
	
	for j = k+1 to n do
		m = max(AVL)
		if (a_j < m) then
			AVL.remove(max)
			AVL.insert(a_j)
	
	return AVL
```